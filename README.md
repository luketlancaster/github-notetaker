# github-notetaker
Following along w/egghead.io

A small app that returns a GitHub user profile and repo list, and allows users to add notes (using firebase)

## To Run:

`git clone`
`npm install`
`webpack -w`

Open the index.html in a browser, or serve using your preferred method.
